using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestPlayer : MonoBehaviour
{
    public PlayerStats playerStats;

    /* Test to see how CollapsableScriptableObjects function in a list.
        In short: they don't. Might be able to search for a fix, who knows
     */
    //public List<PlayerStats> testPlayerStats = new List<PlayerStats>();

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
